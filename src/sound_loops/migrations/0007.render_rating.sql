-- Итерация 5: пользователь оценивает превью в UI как хорошее/нейтральное/
-- плохое. Свойство конкретного рендера (трек мог подойти одному лупу и не
-- подойти другому), не трека и не лупа отдельно — колонка прямо в renders,
-- отдельной таблицы под 1:1-связь не заводим (тот же принцип, что уже
-- применялся к track_segments). NULL — рендер не оценивали.
ALTER TABLE renders ADD COLUMN rating TEXT CHECK (rating IN ('good', 'neutral', 'bad'));
