-- setting — закрытая категория жанра/обстановки для шага B
-- (compose_music_query): не может "утечь" как прилагательное в
-- музыкальный запрос, но достаточна, чтобы отличить "tense" в бою от
-- "tense" в хорроре — одинаковый mood, разная уместная музыка.
--
-- Nullable: старые строки не заполняются задним числом и не участвуют в
-- кеш-lookup'ах (ключ кеша включает prompt_version).
ALTER TABLE video_analyses ADD COLUMN setting TEXT;
